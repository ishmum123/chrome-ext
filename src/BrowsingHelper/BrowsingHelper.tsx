import React from 'react';

function BrowsingHelper() {
    /*const [tabs, setTabs] = useState<string[]>([])

    if (!tabs.length) {
        chrome.topSites.get(sites => setTabs(sites.map(site => site.title).slice(0, 8)))
    }*/

    return <p>hello world</p>;
}

export default BrowsingHelper;
